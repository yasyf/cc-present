{"v":1,"lamport":3,"ops":[{"kind":"set_title","title":"iOS client (ios/CcPresent + CcPresentKit) shipped 2026-07-10 — what remains user-gated, plus local dev gotchas that live nowhere in the repo"},{"kind":"set_body","body":"The native SwiftUI iOS client landed on cc-present main 2026-07-10 (commits: daemon LAN surface, ios/ app + CcPresentKit, docs, ci-ios advisory-style fix), against cc-interact v0.6.0. Still user-gated afterward: TestFlight secrets (`ASC_KEY_ID`/`ASC_ISSUER_ID`/`ASC_KEY_P8`/`APPLE_TEAM_ID`) + App Store Connect app record for `com.yasyf.cc-present`, the one-time Xcode signing-team click, and the physical-iPhone pairing round trip.\n\nLocal gotchas not recorded in the repo:\n- Daemon smoke tests under a temp HOME need a SHORT path (e.g. `/tmp/ccp-smoke`) — `~/.cc-present/daemon.sock` hits the macOS ~104-char `sun_path` limit and fails with `bind: invalid argument` under long scratchpad paths.\n- This machine sets `GOFLAGS=-mod=mod` globally, which conflicts with `go.work` workspace mode; run `env GOFLAGS=-mod=readonly` when a workspace file is present.\n- `xcodebuildmcp` is installed globally via npm as of 2026-07-13 (`npm install -g xcodebuildmcp`, v2.6.2) — there is NO bare brew formula (`brew install xcodebuildmcp` fails; the alternatives are the getsentry tap or npm). Drive builds/tests with `xcodebuildmcp simulator test --scheme CcPresent --simulator-name 'iPhone 17'`.\n- The macos-26 CI runner's brew-latest swiftformat enables rules newer than local 0.62.1 — style steps are `continue-on-error: true` by policy (AGENTS.md: style never blocks).\n\n**Why:** These four cost real debugging time during the iOS-client build-out and none are derivable from the repo.\n\n**How to apply:** Recall when running daemon smokes, Go builds with a workspace, or simulator automation in cc-present. Related: [[cc-interact-release-shape]]."}]}