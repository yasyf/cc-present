{
  "version": 1,
  "title": "Approve the retry plan",
  "submit": { "label": "Send decisions" },
  "blocks": [
    {
      "id": "plan",
      "type": "card",
      "title": "Retry with backoff",
      "summary": "Three attempts with exponential backoff, then surface the last error.",
      "children": [
        {
          "id": "summary",
          "type": "markdown",
          "md": "Wrap the flaky `fetchInventory` call in a retry: three attempts, exponential backoff, then surface the last error."
        },
        {
          "id": "strategy",
          "type": "choice",
          "options": [
            { "id": "jitter", "label": "Full jitter", "hint": "spreads load", "md": "Randomize each delay — smooths thundering herds." },
            { "id": "fixed", "label": "Fixed steps", "hint": "deterministic", "md": "1s, 2s, 4s — predictable, easier to test." }
          ]
        },
        { "id": "verdict", "type": "approval", "prompt": "Ship it?" }
      ]
    }
  ]
}
